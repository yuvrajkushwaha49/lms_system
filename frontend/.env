# In dev, VITE_DEV_PROXY=1 makes the app call /api on the Vite host; vite.config.js forwards to Node.
VITE_DEV_PROXY=1
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5003
# Used when VITE_DEV_PROXY is not 1 (e.g. production build). Point at your API origin.
VITE_API_BASE_URL=http://192.168.100.59:5003
