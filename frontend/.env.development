# Vite dev: proxy /api and /uploads to API gateway (microservices entry)
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5000

# Optional: call gateway directly instead of proxy (set VITE_DEV_PROXY=0)
# VITE_API_BASE_URL=http://localhost:5000
