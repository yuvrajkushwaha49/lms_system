/**
 * Default entry: starts all microservices + API gateway.
 * Dev with auto-reload: npm run dev (nodemon)
 * Production: npm start
 * Monolith fallback: npm run start:monolith
 */
require('./scripts/start-all');
